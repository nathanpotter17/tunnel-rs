# Direct mode.
# No [wireguard] section, so the engine egresses out the host uplink.

tun_ip = "198.18.0.1"   # our address on the TUN (RFC 2544 range, avoids LAN clashes)
tun_prefix = 15
mtu = 1400
dns = "1.1.1.1"
